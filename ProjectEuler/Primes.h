#include <vector>
#include <cmath>

std::vector<bool> getNotprimes(unsigned long limit);
std::vector<int> getPrimes(unsigned long limit);
bool isPrime(int n);
int modExp(unsigned long long b, unsigned long long e, unsigned long long m);